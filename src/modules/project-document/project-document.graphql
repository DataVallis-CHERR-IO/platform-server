type Query {
    projectDocuments(projectId: String!): [ProjectDocument]
}

type ProjectDocument {
    _id: String
    projectId: String
    title: String
    path: String
    icon: String
    format: String
    statusId: Int
    createdAt: String
    updatedAt: String
    deletedAt: String
}
