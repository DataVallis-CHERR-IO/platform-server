type Query {
    projectDetail(projectId: ID!): ProjectDetail!
}

type Mutation {
    createProjectDetail(projectId: ID!, description: String!, requirements: String): ProjectDetail!
}

type ProjectDetail {
    _id: String
    projectId: String
    description: String
    requirements: String
    createdAt: String
    updatedAt: String
}
