type Query {
    projectDetail(projectId: String!): ProjectDetail!
}

type Mutation {
    createProjectDetail(projectId: String!, description: String!, requirements: String): ProjectDetail!
}

type ProjectDetail {
    _id: String
    projectId: String
    description: String
    requirements: String
    createdAt: String
    updatedAt: String
}
