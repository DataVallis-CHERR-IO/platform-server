type Query {
    campaignDetail(campaignId: String): CampaignDetail
}

type CampaignDetail {
    _id: String
    campaignId: String
    requirements: String
    description: String
    createdAt: String
    updatedAt: String
}
