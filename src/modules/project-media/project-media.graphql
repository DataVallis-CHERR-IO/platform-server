type Query {
    projectMedia(projectId: String!): [ProjectMedia]
}

type ProjectMedia {
    _id: String
    projectId: String
    title: String
    description: String
    path: String
    image: String
    statusId: Int
    createdAt: String
    updatedAt: String
    deletedAt: String
}
